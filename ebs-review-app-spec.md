# 입트영 복습 앱 — 기능 명세 (MVP)

## 프로젝트 개요
- 목적: EBS 입트영 교재로 공부한 문장을 소리 내어 복습하는 개인용 학습 앱
- 사용자: 본인 1인 전용 (회원가입/로그인 없음)
- 사용 기기: **안드로이드폰(주력) / 아이패드(보조)** — 두 기기 모두 홈화면에 설치해서 앱처럼 사용
- 형태: **PWA (홈화면에 추가하는 웹앱)** — 백엔드 서버 없음, 모든 데이터는 기기 안에 저장
- 비용 목표: 실질적 $0 (호스팅은 GitHub Pages 무료, Speech 사용량은 UNH 학생 계정의 Azure $100 크레딧으로 충당 — S0 표준 등급 사용)

## 아키텍처 (중요 — 이 구조를 반드시 따를 것)
- **백엔드 서버를 만들지 말 것.** 정적 파일(HTML/CSS/JS)만으로 구성한다.
- 호스팅: GitHub Pages (무료, HTTPS 자동 제공)
  - 마이크 사용에는 HTTPS가 필수라 `file://`로 직접 여는 방식은 작동하지 않음
  - **무료 계정은 공개(public) 저장소에서만 Pages를 쓸 수 있음.** 즉 코드는 전 세계에 공개된다
    → 그래서 키를 코드에 넣지 않는 설계가 필수. `.env`, 키가 담긴 파일은 `.gitignore`에 등록하고, 커밋 전에 키 문자열이 들어갔는지 확인할 것
  - 프로젝트 사이트 주소는 `https://<계정>.github.io/<저장소명>/` 형태의 **하위 경로**임. 따라서:
    - `vite.config.js`에 `base: '/<저장소명>/'` 설정 필수
    - `manifest.json`의 `start_url`, `scope`, 아이콘 경로도 모두 같은 하위 경로 기준으로 작성
    - 서비스워커 등록 경로도 동일 기준. 이 설정을 빠뜨리면 배포 후 흰 화면만 뜨고 아무것도 안 보임 (가장 흔한 실패 지점)
- Azure API 키: 앱의 **설정 화면에서 사용자가 직접 입력**하고 기기 저장소에 보관
  - 코드나 저장소(repository)에는 키를 절대 넣지 않는다
  - 키가 브라우저 안에만 있으므로, URL을 아는 사람이 있어도 자기 키 없이는 사용 불가 → 별도 비밀번호 불필요
- 음성 처리: **Azure Speech SDK for JavaScript를 브라우저에서 직접 사용**
  - **오디오 입력은 푸시 스트림(push stream) 방식으로 구현할 것:**
    `getUserMedia`로 마이크 스트림 획득 → `AudioContext` + `AudioWorklet`으로 **16kHz / 모노 / 16bit PCM**으로 변환 →
    `AudioInputStream.createPushStream()`에 밀어 넣고 `AudioConfig.fromStreamInput()`으로 인식기에 연결
  - 이렇게 하는 이유 두 가지:
    1. `AudioConfig.fromDefaultMicrophoneInput()`은 **일부 안드로이드 기기에서 마이크가 켜진 것처럼 보이지만 무음만 전달되는 문제**가 보고돼 있음 (에러도 안 뜸). 직접 스트림을 다루면 이 문제를 피할 수 있고, 원인 파악도 가능
    2. 기능 2의 "내 녹음 다시 듣기"를 구현하려면 오디오 원본이 손에 있어야 하는데, SDK 마이크 입력 방식은 오디오를 돌려주지 않음. 푸시 스트림 방식이면 같은 PCM 데이터를 복사해 WAV로 만들어 재생·저장할 수 있음
  - MediaRecorder로 녹음한 WebM 파일을 Azure에 그대로 보내는 방식은 거부되므로 사용하지 말 것
  - 구현 중 마이크가 무음으로 잡히면, 화면에 입력 레벨(음량 막대)을 표시해 즉시 확인할 수 있게 할 것
- PWA 필수 설정: `manifest.json`에서 **`"display": "standalone"`**
  - 안드로이드 Chrome에서 정식 설치(홈화면 추가)가 가능해지고, 아이패드에서 데이터가 7일 후 삭제되는 것을 막아줌. 반드시 포함할 것
  - 안드로이드 Chrome에서 설치 프롬프트가 뜨려면 manifest에 `name`, `short_name`, `start_url`, 192px·512px 아이콘, 서비스워커가 모두 있어야 함

## 데이터 저장
- IndexedDB 사용 (기기 안에 영구 저장, 접속할 때마다 초기화되지 않음)
- `navigator.storage.persist()`로 저장소 보호 요청
  - 페이지 로딩 시점이 아니라 **카드를 처음 저장하는 등 사용자가 버튼을 누른 직후에 호출**할 것 (브라우저가 거부할 확률이 낮아짐)
  - 안드로이드 Chrome은 앱을 설치(홈화면 추가)해두면 대체로 승인해줌
  - 설정 화면에 `navigator.storage.persisted()` 결과를 표시해 승인 여부를 확인할 수 있게 할 것
- **JSON 내보내기 / 가져오기 기능 필수**
  - 폴더 구조와 폴더별 설정까지 함께 담을 것 (카드만 옮기면 폴더가 사라짐)
  - 백업 용도, 그리고 안드로이드폰↔아이패드 간 데이터 옮기기 용도
  - 기기마다 저장소가 분리되므로 자동 동기화는 없음

## 핵심 기능

### 1. 폴더 관리

카드를 주제별로 나눠 담고, 폴더 단위로 따로 복습한다. (예: `입트영`, `원서 표현`, `대학원 영어`)

- 폴더 추가 / 이름 변경 / 순서 변경 / 삭제
- **앱 첫 화면 = 폴더 목록.** 각 폴더에 다음을 표시:
  - 폴더 이름
  - 오늘 학습할 카드 수 (예: `오늘 42장`)
  - 전체 카드 수 / 완료한 카드 수
- 폴더를 누르면 그 폴더의 오늘의 학습이 시작됨
- 카드는 반드시 하나의 폴더에 속한다. 카드를 **다른 폴더로 옮기는 기능** 제공
- 폴더 삭제 시 안에 든 카드를 어떻게 할지 반드시 물어볼 것 (함께 삭제 / 다른 폴더로 옮기기)
- 앱을 처음 켰을 때 폴더가 하나도 없으면, 카드 추가 화면으로 보내기 전에 폴더부터 만들도록 안내
- 폴더마다 색을 지정한다 (아래 디자인 항목의 폴더 색 팔레트 참고)
  - 폴더를 만들 때 팔레트에서 자동으로 아직 안 쓴 색이 배정되고, 언제든 바꿀 수 있음
- 디자인: 노트 표지나 색인 탭처럼, 폴더 이름은 세리프체로 크게

### 2. 문장 카드 관리
- 새 카드 추가: 한국어 문장 + 정답 영어 문장을 직접 타이핑해서 입력
- 카드 목록 보기 / 수정 / 삭제
- **복습 횟수 배지**: 카드 오른쪽 위에 지금까지 복습한 횟수를 작게 표시 (`attempt_count` 값)
  - 목록 화면과 복습 화면 양쪽에 모두 표시
  - 조용한 정보이므로 `rule` 색의 작은 숫자로, 강조하지 않게 처리
  - 아직 한 번도 안 본 카드(`new`)는 숫자 대신 "새 카드"로 표시

### 3. 복습(리뷰) 화면
- 한국어 문장 표시 → 마이크 버튼을 눌러 영어로 답변 녹음
- Azure가 인식한 내 문장 텍스트를 정답과 나란히 표시 (어느 단어를 빠뜨렸는지/잘못 말했는지 확인)
- 발음/억양 점수 표시
- 내 녹음 다시 듣기

### 4. 발음/억양 채점
- Azure Speech Pronunciation Assessment 사용, 정답 영어 문장을 참조 텍스트(ReferenceText)로 전달
- 표시 항목: 정확도(Accuracy) / 유창성(Fluency) / 완성도(Completeness) / 운율(Prosody — 억양·강세·속도)
- 설정: `GradingSystem: HundredMark`, `Granularity: Phoneme`, `EnableMiscue: true`, `EnableProsodyAssessment: true`
- **운율(Prosody) 점수가 계정·리전에 따라 안 나올 수도 있음.** 값이 안 오면 해당 항목을 화면에서 숨기고 나머지 점수만 표시하도록 처리할 것

### 5. 타이핑 답변 모드

소리를 낼 수 없는 상황(지하철, 도서관, 늦은 밤)에서도 같은 카드를 복습할 수 있게 하는 대체 입력 방식.

**전환 방법**
- 복습 화면 상단에 말하기 / 타이핑 전환 버튼 — 복습 도중에도 카드 단위로 바꿀 수 있음
- 폴더 설정에 "기본 답변 방식"을 두어 폴더마다 기본값 지정 (기본값: 말하기)

**동작**
- 한국어 문장을 보고 영어 문장을 입력창에 타이핑 → 정답과 비교해 채점
- 정답과 다른 부분을 단어 단위로 표시 (빠뜨린 단어, 틀린 단어, 덧붙인 단어)
- 이 모드에서도 **원어민 발음 듣기는 그대로 사용 가능** (캐시된 음성은 인터넷 없이도 재생됨)
- **인터넷 없이 완전히 작동함** — Azure를 호출하지 않으므로 비행기·지하철에서도 채점까지 가능

**채점 방식**
- 비교 전 정규화: 소문자로 변환, 문장부호 제거, 연속 공백 정리
- 점수 = 단어 단위 편집 거리 기준 `(1 - 편집거리 / 정답 단어 수) × 100`, 0 미만은 0으로
- 이 점수를 기존 SM-2 등급표(90/80/70/60 구간)에 그대로 적용
- 대소문자·마침표 차이만으로 감점하지 않는다

**모바일 입력창 설정 (반드시 지킬 것)**
- 안드로이드 키보드의 자동 수정이 오답을 정답으로 바꿔버려 점수가 부풀려짐. 다음을 모두 끌 것:
  `autocorrect="off"`, `autocapitalize="off"`, `spellcheck="false"`, `lang="en"`
- 한국어 문장이 위에 있어도 입력창은 영문 키보드가 뜨도록 처리

**기록 처리**
- 타이핑으로 한 복습도 `attempt_count`에 포함된다 (배지 숫자에 반영)
- 다만 발음 점수와 성격이 다르므로, 각 기록에 **입력 방식(말하기/타이핑)을 함께 저장**한다
- 복습 화면의 점수 표시에도 어느 방식으로 받은 점수인지 작게 표기

### 6. 오늘의 학습 모드

**폴더 하나를 골라 시작한다.** 오늘 볼 카드 묶음을 만들어 순서대로 넘기며 복습한다.

**하루 목표 개수 설정**
- **폴더마다 따로 설정한다** (입트영은 매일 100장, 대학원 영어는 20장처럼 분량이 다르므로)
- 선택: 50 / 100 / 150 / 200 (직접 입력도 허용), 기본값 50
- 폴더 설정 화면에서 변경

**오늘의 카드 묶음 구성 순서** (선택한 폴더 안의 카드만 대상)
1. **새 카드(`new`)를 먼저 채운다** — 등록한 순서(오래된 것부터)
2. 남은 자리를 **복습이 필요한 카드**(`review_due_date` <= 오늘, 상태가 `learning`)로 채운다 — 아래 우선순위 점수가 높은 순
3. 그래도 목표 개수에 못 미치면, 아직 복습일이 안 된 카드는 **끌어오지 않는다.** 목표보다 적은 개수로 그날 학습을 끝낸다
   - (이유: 억지로 채우면 간격 반복의 효과가 떨어짐. "오늘은 30개로 끝"이 정상 동작임을 화면에 안내)
4. 새 카드만으로 이미 목표를 넘으면, 넘치는 새 카드는 다음 날로 미룬다

**우선순위 점수 (복습 카드 정렬 기준)**

각 항목을 0~100으로 계산한 뒤 가중치를 곱해 더한다. 높을수록 먼저 나온다.

| 항목 | 계산 | 가중치 |
|---|---|---|
| 밀린 정도 | `min(오늘 - review_due_date, 30) / 30 × 100` | 0.4 |
| 틀린 비율 | `fail_count / max(attempt_count, 1) × 100` | 0.3 |
| 최근 점수 낮음 | `100 - last_avg_score` (기록 없으면 100) | 0.2 |
| 복습 부족 | `max(0, 5 - attempt_count) / 5 × 100` | 0.1 |

- 동점이면 먼저 등록한 카드(`created_at`)가 앞에 온다
- 플래그(어려움)된 카드는 계산된 점수에 **+15 가산**

**학습 세션 유지**
- 세션은 **폴더별로 하루에 하나씩** 만든다 (입트영 세션과 원서 세션은 서로 독립적으로 진행)
- 오늘 만든 묶음은 그날 하루 동안 고정한다 (앱을 껐다 켜도 같은 목록, 이어서 진행)
- 진행 상황(몇 번째까지 했는지)을 저장해 중단한 지점부터 재개
- 화면 상단에 "12 / 50" 형태의 진행 표시
- 다 끝내면 완료 화면을 보여주고, 원하면 "더 하기"로 추가 복습 가능
- 날짜가 바뀌면(기기 로컬 시간 자정 기준) 새 묶음을 만든다

### 7. 간격 반복 스케줄링 (SM-2)
- 카드 상태: `new`(아직 안 본 카드) / `learning`(학습 중) / `completed`(암기 완료)
- 발음 점수를 SM-2 등급(0~5)으로 변환하는 규칙:

  | 종합 점수 | SM-2 등급 | 처리 |
  |---|---|---|
  | 90 이상 | 5 | 아주 잘함 |
  | 80~89 | 4 | 잘함 |
  | 70~79 | 3 | 통과 |
  | 60~69 | 2 | 실패 — 주기 초기화 |
  | 60 미만 | 1 | 실패 — 주기 초기화 |

  - 종합 점수 = 정확도·유창성·완성도의 평균 (운율 점수가 있으면 함께 평균)
  - 인식 자체가 안 된 경우(NoMatch)는 등급을 매기지 않고 재시도 처리 (아래 주의사항 참고)
  - 등급 3 미만이면 `repetitions`를 0으로, `interval`을 1일로 초기화
  - 자동 채점이 이상하다고 느낄 때를 대비해 **수동으로 등급을 덮어쓰는 버튼**도 제공 ("다시 볼래요" / "알겠어요")
- "오늘 복습할 카드" 목록 자동 생성 (`review_due_date` <= 오늘, 기기 로컬 시간 기준)

### 8. 플래그 (어려운 문장 표시)
- 카드마다 "어려움" 토글 버튼
- "어려운 문장 모음" 화면에서 플래그된 카드만 모아보기 (완료된 카드도 플래그돼 있으면 여기 표시)
- 폴더별로 보기 / 전체 폴더 합쳐 보기 둘 다 가능

### 9. 암기 완료 처리
- "완료"로 표시하면 일일 복습 목록에서 제외되고 그 폴더의 "완료된 문장" 보관함으로 이동
- 완료된 카드도 언제든 수동으로 복습 목록에 되돌리기 가능

### 10. 원어민 발음 듣기
- 카드마다 "정답 발음 듣기" 버튼
- 미국식 / 영국식 선택 — 앱 전체 설정으로 저장 (카드별 아님)
  - 미국식: `en-US-AvaMultilingualNeural` 또는 `en-US-AndrewMultilingualNeural`
  - 영국식: `en-GB-SoniaNeural` 또는 `en-GB-RyanNeural`
- Azure Neural TTS 사용 (발음 평가와 같은 Speech 리소스, 별도 연동 불필요)
- 생성된 음성은 IndexedDB에 캐시해서 같은 문장을 다시 들을 때 재요청하지 않도록 함 (무료 할당량 절약 + 속도)

## 디자인 방향

콘셉트: **개인 학습 노트(줄노트/워크북)를 넘겨보는 느낌.** 화려한 SaaS 대시보드가 아니라, 조용하고 정돈된 나만의 복습 노트처럼 보이는 것이 목표. 아래 값은 고정이 아니라 방향이므로, Claude Code가 구현하면서 미세 조정 가능.

**색상 (라이트 모드)**
| 이름 | hex | 용도 |
|---|---|---|
| paper | #EEF0EA | 배경 (차가운 세이지 톤 종이) |
| ink | #24282B | 본문 텍스트 |
| surface | #F8F9F5 | 카드 표면 (배경보다 살짝 밝게) |
| rule | #C9CDBF | 구분선 (노트 줄 느낌) |
| accent | #3F6752 | 주요 액션·정답·진행 표시 (차분한 잉크그린) |
| flag | #A6572E | 플래그(어려움) 표시 전용, 이 색은 다른 곳에 쓰지 않음 |

**색상 (다크 모드)**
| 이름 | hex | 용도 |
|---|---|---|
| paper | #1B1D19 | 배경 |
| ink | #E7E9E2 | 본문 텍스트 |
| surface | #24261F | 카드 표면 |
| rule | #3A3D33 | 구분선 |
| accent | #7FA98D | 주요 액션·정답·진행 표시 |
| flag | #D98657 | 플래그 표시 전용 |

**타이포그래피**
- 한국어 문장(복습 화면에서 크게 보여주는 제시 문장): 세리프 — `Noto Serif KR` (교재 본문 같은 느낌)
- 그 외 UI 전체(버튼, 라벨, 영어 인식 결과, 점수): 산세리프 — `Pretendard`
- 이렇게 나누는 이유: "지금 외워야 할 내용"과 "앱 조작 요소"가 눈에 바로 구분되도록
- **두 폰트 모두 안드로이드에 기본 설치돼 있지 않으므로 웹폰트로 불러와야 함.** 한글 웹폰트는 용량이 크니 다음을 지킬 것:
  - `font-display: swap` 적용 (폰트 로딩 중에도 글자가 보이도록)
  - 대체 글꼴 지정: `'Pretendard', 'Noto Sans KR', system-ui, sans-serif`
  - 서브셋(사용 글자만 추린 경량 버전) 또는 동적 서브셋 방식 사용

**레이아웃**
- 모바일 세로 화면, 한 번에 카드 한 장만 보여주는 단일 컬럼 구성
- 카드는 둥근 사각형 + 그림자를 겹겹이 쓰는 흔한 SaaS 카드 스타일 대신, **모서리를 살짝만 둥글게(4px) + 그림자 없이 얇은 실선 테두리**로 마감 (실제 색인카드 느낌)
- 한국어 문장 아래에 노트 밑줄처럼 `rule` 색 가로선 하나만 사용 — 장식이 아니라 "여기까지가 오늘의 문제"라는 구분 정보로 사용
- 버튼 문구는 평서형 동사로: "녹음하기", "다시 듣기", "완료로 표시" (끝에 화살표(→)나 영어식 "Submit" 금지)

**폴더 색 팔레트**

책등이나 색인 탭에 쓰는 물 빠진 천 색을 기준으로 함. 여섯 색 모두 채도를 낮게 맞춰 어느 하나가 튀지 않게 한다.

| 이름 | 라이트 | 다크 |
|---|---|---|
| 흐린 청 | #4E6A7D | #8AA6B7 |
| 남보라 | #55597E | #9095B8 |
| 자두 | #6E5068 | #AB8CA5 |
| 황토 | #8A6E3B | #C3A66E |
| 이끼 | #5E7355 | #93A788 |
| 안개회 | #5A6068 | #979CA4 |

**폴더 색을 쓰는 위치 (이 범위를 넘지 말 것)**
- 폴더 목록에서 각 항목 왼쪽의 색인 탭 모양 세로 막대 (폭 4px 정도)
- 카드 왼쪽 가장자리의 얇은 세로 선 — "이 카드가 어느 폴더 것인지"만 알려주는 용도
- 폴더 이름 옆의 작은 점 표시

**폴더 색으로 하지 말 것**
- 카드나 폴더 배경 전체를 그 색으로 칠하기
- 버튼, 진행 표시, 점수 표시에 사용하기 — 이 자리는 `accent` 색 고정
- 플래그(어려움) 표시에 사용하기 — 이 자리는 `flag` 색 고정
- 즉 **폴더 색·accent·flag는 서로 다른 자리에만 나타난다.** 색끼리 비슷해 보여도 위치가 달라 헷갈리지 않음

**하지 말 것 (Claude Code가 기본값으로 흘러가기 쉬운 것들)**
- 모든 카드에 동일한 둥근 모서리 + 은은한 회색 그림자를 반복하는 "SaaS 카드 키트" 스타일
- 라벨 앞에 트래킹 넓은 ALL-CAPS eyebrow, 가운데점(·)으로 이은 메타 정보 문구
- 그라데이션 장식, 아이콘마다 다른 원색 남발

## 데이터 모델 (카드 기준)
| 필드 | 설명 |
|---|---|
| id | 카드 고유 번호 |
| folder_id | 속한 폴더 |
| korean_text | 한국어 문장 |
| english_text | 정답 영어 문장 |
| created_at | 생성일 |
| status | new / learning / completed |
| flagged | true / false |
| review_due_date | 다음 복습 예정일 |
| interval | 다음 복습까지 일수 (SM-2, 초기값 0) |
| repetitions | 연속 통과 횟수 (SM-2, 초기값 0) |
| ease_factor | 난이도 계수 (SM-2, 초기값 2.5, 최솟값 1.3) |
| last_scores | { accuracy, fluency, completeness, prosody } 최근 채점 결과 |
| last_avg_score | 최근 종합 점수 (우선순위 계산용, 0~100) |
| last_mode | 최근 복습의 입력 방식 (`speak` / `type`) |
| attempt_count | 총 복습 횟수 — **카드에 표시되는 배지 숫자.** 인식 실패(NoMatch)는 세지 않음 |
| fail_count | SM-2 등급 3 미만이 나온 횟수 (우선순위 계산용) |

주의: `attempt_count`(총 복습 횟수, 화면에 보이는 값)와 `repetitions`(SM-2의 연속 통과 횟수, 내부 계산용)는 서로 다른 값이다. 혼동하지 말 것.

## 데이터 모델 (폴더)
| 필드 | 설명 |
|---|---|
| id | 폴더 고유 번호 |
| name | 폴더 이름 |
| color | 폴더 색 (팔레트 6색 중 하나) |
| daily_target | 이 폴더의 하루 목표 개수 (기본 50) |
| default_mode | 기본 답변 방식 (`speak` / `type`, 기본 `speak`) |
| sort_order | 첫 화면에서의 표시 순서 |
| created_at | 생성일 |

## 앱 설정 (전역, 폴더와 무관)
| 항목 | 설명 |
|---|---|
| azure_key / azure_region | Azure 접속 정보 |
| accent | `en-US` / `en-GB` |
| theme | 라이트 / 다크 / 시스템 따라가기 |

## 오늘의 학습 세션 (폴더별로 하루 한 건 저장)
| 항목 | 설명 |
|---|---|
| folder_id | 어느 폴더의 세션인지 |
| date | 세션 날짜 (기기 로컬 시간 기준) |
| card_ids | 오늘 묶음의 카드 순서 |
| current_index | 진행 위치 |
| completed | 오늘 학습 완료 여부 |

## 기술 스택
- 프론트엔드: **React + Vite** (단일 페이지 앱)
- 저장소: IndexedDB (idb 등 간단한 래퍼 사용 가능)
- 음성: Azure Speech SDK for JavaScript (`microsoft-cognitiveservices-speech-sdk`)
  - Vite 번들링 시 `global is not defined` 류의 오류가 날 수 있음. 발생하면 `vite.config.js`에 `define: { global: 'globalThis' }` 추가로 해결
- 호스팅: GitHub Pages
- 백엔드/DB 서버: 없음
- 대상 브라우저: 안드로이드 Chrome(주력), iPadOS Safari(보조) — 두 곳 모두에서 동작 확인할 것

## MVP 범위에서 제외
- 다중 사용자, 회원가입/로그인
- 기기 간 자동 동기화 (JSON 내보내기/가져오기로 대체)
- 콘텐츠 자동 수집(스크래핑) — 저작권 있는 교재이므로 직접 타이핑만 지원
- 소셜/공유 기능
- 네이티브 모바일 앱 (App Store 배포)

## 안드로이드·배포 관련 주의사항

- **채점 요청이 진행 중일 때는 녹음/채점 버튼을 비활성화**해 요청이 겹치지 않게 처리할 것 (중복 요청은 크레딧을 불필요하게 소모함)
- **크레딧 잔액 확인 습관화**: S0(표준) 등급은 쓴 만큼 $100 크레딧에서 자동 차감되므로, Azure Sponsorships 포털에서 가끔 잔액을 확인할 것. 앱에 잔액 표시 기능을 넣을 필요는 없음
- **인식 실패(NoMatch) 처리**: 소음이나 무음으로 Azure가 아무것도 인식하지 못했을 때는 이를 "0점 오답"으로 기록하지 말 것. 복습 주기를 망가뜨림.
  대신 "잘 들리지 않았어요. 다시 녹음해 주세요"를 띄우고 카드 일정은 그대로 둔다
- **오프라인 동작**: 카드 보기·추가·수정, 캐시된 원어민 음성, **타이핑 답변 모드의 채점까지** 인터넷 없이 작동해야 함. 말하기 채점과 새 음성 생성만 인터넷이 필요하다
  - 인터넷이 없을 때는 말하기 대신 **타이핑 모드로 전환하라고 안내**할 것 (그냥 막지 말 것)
- **서비스워커 업데이트**: 새 버전을 배포해도 설치된 PWA가 예전 화면을 계속 보여줄 수 있음. 새 버전 감지 시 "새 버전이 있습니다 — 새로고침" 안내를 띄우도록 처리할 것
- **마이크 권한**: 안드로이드 Chrome은 HTTPS에서만 마이크를 허용함. 사용자가 권한을 거부한 경우 앱이 조용히 멈추지 말고, 안내 문구와 재요청 버튼을 보여줄 것

## 개발 순서 (Claude Code로 단계별 진행)

**0단계 (직접 준비 — 코딩 전에 필요)**
1. (완료) UNH 학생 계정으로 Azure for Students 가입, $100 크레딧 확보
2. Azure Portal에서 Speech 리소스 생성, 가격 등급은 **S0(표준)** 선택 — F0(무료)가 아님. 월 5시간 한도 없이, 쓴 만큼 크레딧에서 자동 차감되게 하기 위함
3. 키(Key)와 지역(Region, 예: `eastus`) 복사해두기
4. GitHub 계정 생성
5. 컴퓨터에 Node.js LTS 버전 설치 (Claude Code가 프로젝트를 만들고 실행하는 데 필요)

**1~7단계 (Claude Code로 진행)**
1. React + Vite 프로젝트 생성, `base` 경로 설정, 디자인 방향의 색상·폰트를 CSS 변수로 세팅, PWA manifest 설정(`display: standalone`), GitHub Pages 배포 후 **안드로이드폰에서 실제로 열리는지 확인**하고 나서 다음 단계로
2. 설정 화면: Azure 키·지역 입력 및 저장, 미국식/영국식 선택
3. 폴더 CRUD + 첫 화면(폴더 목록), 카드 CRUD(추가/조회/수정/삭제) + IndexedDB 저장
4. Azure SDK 연동 — 녹음 → 발음 평가 → 점수 표시, 타이핑 답변 모드와 전환 버튼
5. 원어민 발음 듣기(TTS) + 음성 캐시
6. SM-2 로직 + 우선순위 점수 구현, 폴더별 "오늘의 학습" 묶음 생성·세션 유지·진행 표시 완성
7. 복습 횟수 배지 / 플래그 / 완료 처리 / 카드 폴더 이동 / JSON 내보내기·가져오기

각 단계가 끝날 때마다 안드로이드폰에서 실제로 열어 동작을 확인한 뒤 다음 단계로 진행할 것. 아이패드 확인은 4단계(음성 기능) 이후 한 번 몰아서 해도 됨.
